/** @type {import('next').NextConfig} */
const nextConfig = {
  // Blog images are now served locally from /public/blog/*, so no remote
  // image hosts are required. Add remotePatterns here if that changes.
};

module.exports = nextConfig; 